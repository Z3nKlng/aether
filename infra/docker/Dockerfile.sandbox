FROM node:22-alpine AS runner
WORKDIR /app
RUN addgroup --system --gid 1001 aether && adduser --system --uid 1001 aether
RUN apk add --no-cache git bash curl
USER aether
ENV NODE_ENV=production
CMD ["bash"]
